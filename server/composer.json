```json
{
    "name": "collaborative-tasks/api",
    "description": "Backend API for the Collaborative Task Management System. Manages tasks, projects, and user authentication. Designed to integrate with a larger ecosystem including Real-time Collaborative Whiteboard, Micro Social Media Dashboard, and Multi-vendor E-commerce Marketplace.",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Your Company",
            "email": "dev@yourcompany.com"
        }
    ],
    "require": {
        "php": "^8.1",
        "slim/slim": "^4.12",
        "slim/psr7": "^1.6",
        "vlucas/phpdotenv": "^5.6",
        "illuminate/database": "^10.48",
        "illuminate/events": "^10.48",
        "monolog/monolog": "^3.5",
        "tuupola/slim-jwt-auth": "^3.4",
        "middlewares/slim-cors": "^2.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "post-root-package-install": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ],
        "start": "php -S 0.0.0.0:8000 -t public",
        "test": "phpunit"
    },
    "config": {
        "allow-plugins": {
            "php-http/discovery": true
        },
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
```